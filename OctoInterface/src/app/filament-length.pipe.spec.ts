import { FilamentLengthPipe } from './filament-length.pipe';

describe('FilamentLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new FilamentLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
