import { PrintTimePipe } from './print-time.pipe';

describe('PrintTimePipe', () => {
  it('create an instance', () => {
    const pipe = new PrintTimePipe();
    expect(pipe).toBeTruthy();
  });
});
